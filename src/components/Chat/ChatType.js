import React from 'react';

const ChatType = (explain, write) => {
    return (
        <div className="chat-type">
            <button>문구 해석</button>
            <button>문장 작성</button>
        </div>
    );
}

export default ChatType;